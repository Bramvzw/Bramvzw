name: generate profile assets

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: Bramvzw
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=%23f97316
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=%23f97316
      - name: generate profile card
        run: python3 scripts/generate-profile-card.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: deploy to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
